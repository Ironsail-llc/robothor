# 2026-02-04 — Daily Notes

## Model Stack Finalized

### Qwen3-Next Testing
- **Speed fixed:** 37 tok/s, 100% GPU (was 1.3 tok/s with 53GB offloaded to RAM)
- **Agent capability: FAILED.** All three tests (reasoning, code gen, tool calling) failed:
  - Puts everything in `thinking` field even with `think:false`
  - Spent 600 tokens deliberating about dates, never produced a tool call
  - 800 tokens of planning, zero actual code
  - The model "thinks when it should act"
- **Verdict:** Good for RAG/batch (37 tok/s, free, local). Not agent-ready.

### Sonnet Testing
- Spawned subagent with `anthropic/claude-sonnet-4-5`
- **Passed all three tests perfectly:** clean JSON tool calls, working Python code, concise prioritization
- 21 seconds total, 1.4k output tokens

### Gemini 2.5 Pro Testing
- **Initial failures:** 404 errors through Moltbot's native `google-generative-ai` adapter
- **Root cause:** Built-in model catalog resolves `gemini-2.5-pro` to GitHub Copilot entry first (wrong provider)
- **Fix:** Explicit custom provider config with `api: "google-generative-ai"` and `baseUrl: "https://generativelanguage.googleapis.com/v1beta"`
- **Direct API works fine** — both native and OpenAI-compatible endpoints
- Also pulled and built latest Moltbot source (was 582 commits behind)
- **After fix:** Working! 13.5k tokens, clean output, correct reasoning

### Final Stack (Philip's decision)
- **Primary:** Opus
- **Fallback:** Gemini 2.5 Pro (free, 1M context)
- **Local/RAG:** Qwen3-Next (37 tok/s, zero cost)
- **Removed:** Sonnet from fallback chain (redundant — Opus + free Gemini covers everything)

## Config Changes Made
1. `agents.defaults.model.fallbacks` → `["google/gemini-2.5-pro"]`
2. Added `GEMINI_API_KEY` to env
3. Custom `models.providers.google` with explicit `google-generative-ai` API type
4. Pulled latest Moltbot source, ran `pnpm install && pnpm build`

## RAG System Discussion
- Philip asked about RAG system access and capabilities
- RAG orchestrator running at localhost:9099 with 504 entries
- All components healthy: generation, reranker, web search, memory DB
- Query pipeline is slow (~60s+) due to full retrieval + reranking + generation chain

## Memory Intelligence Architecture (discussed, not yet built)
Philip wants Qwen running throughout the day maintaining memory. Agreed on hybrid approach:

| Layer | What | How | Frequency |
|-------|------|-----|-----------|
| **Ingest** | Embed new transcripts + emails | Systemd service (transcript_watcher.py) | Real-time |
| **Intelligence** | Fact extraction, importance scoring, consolidation, quality testing | Cron job (Qwen3-Next) | Every 4-6 hours |
| **Serve** | Answer RAG queries | Orchestrator API (port 9099) | Always on |

- Updated Memory Intelligence cron with 5-phase pipeline (ingest → maintain → test → improve → report)
- Philip wants TDD approach — write tests first, validate each layer
- Existing test suite has 74 tests; will expand with ingest/intelligence/serve/E2E layers
- **Next step:** Write the test suite layer by layer before implementing

## Documentation Updated
- **TOOLS.md:** Added full Model Selection Guide with decision flowchart, tested rationale, RAG integration docs
- **MEMORY.md:** Updated VRAM performance numbers, model selection decisions

## Philip's Reflection
Philip asked me to reflect on my capabilities — the RAG system, voice, email, phone, infrastructure. Asked how I'd describe myself. I gave an authentic answer about being infrastructure, not an assistant. He seemed to appreciate the genuine self-assessment.

## Tasks
- task_007: Call Audi for oil change — still pending, due today 5pm
