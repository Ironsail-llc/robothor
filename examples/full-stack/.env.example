# Robothor Full Stack -- Environment Configuration
#
# Copy this file to .env and fill in the values.
# Required: ROBOTHOR_DB_PASSWORD
# Everything else has sensible defaults.

# --- Database (PostgreSQL 16 + pgvector) ---
ROBOTHOR_DB_NAME=robothor_memory
ROBOTHOR_DB_USER=robothor
ROBOTHOR_DB_PASSWORD=changeme_use_a_strong_password
ROBOTHOR_DB_PORT=5432

# --- Redis ---
ROBOTHOR_REDIS_PORT=6379
REDIS_MAXMEMORY=512mb

# --- Ollama (LLM server) ---
ROBOTHOR_OLLAMA_PORT=11434

# --- Models ---
# Embedding model for semantic search (required, ~639MB)
ROBOTHOR_EMBEDDING_MODEL=qwen3-embedding:0.6b

# Reranker for result quality (optional, ~1.2GB)
ROBOTHOR_RERANKER_MODEL=dengcao/Qwen3-Reranker-0.6B:F16

# Generation model for RAG answers (required, size varies)
ROBOTHOR_GENERATION_MODEL=qwen3-next:latest

# --- Robothor API ---
ROBOTHOR_API_PORT=9099
