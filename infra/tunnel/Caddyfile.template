# Caddy Reverse Proxy Config
# Generated by: robothor tunnel generate --provider caddy
# Caddy handles automatic HTTPS via Let's Encrypt.

engine.${DOMAIN} {
    reverse_proxy localhost:18800
}

app.${DOMAIN} {
    reverse_proxy localhost:3004
}

bridge.${DOMAIN} {
    reverse_proxy localhost:9100
}

vision.${DOMAIN} {
    reverse_proxy localhost:8600
}

${TTS_BLOCK}
${MONITORING_BLOCK}
${CAMERA_BLOCK}
